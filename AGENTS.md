OnFire 是一个从简设计的现代工单系统，旨在让用户可以快速的创建工单，并且提供给开发团队一个便利的访问、查看的 dashboard，以及具有完善权限管理的工单管理和回复能力。

## 技术栈

OnFire 将使用前后端分离的模式，后端使用 bun + elysia + cloudflare worker / cloudflare D1 + TypeScript，前端使用 React + shadcn/ui。

我们会将所有的内容放在一个 monorepo 中，这个 monorepo 使用 turborepo 来管理。

请你使用 ElysiaJS 的 Cloudflare Worker adapter，同时接入 Better Auth 管理登录。

## 架构设计

OnFire 分为两个部分，一个是 ToC 的部分，主要是提供 SDK 给到各个产品进行接入，用于给用户提供提交工单、查询过往工单的能力，我们可以提供完善的组件，也需要在项目内直接提供对应的页面供用户使用。另一部分是 ToB，用于给内部客服团队使用。其中系统内置的 ToC 页面和 ToB 需要各自构建独立的前端、后端服务，作为两个不同的 Cloudflare Worker 进行部署，二者共享同一个数据库。

### ToB

ToB 部分的前端我们需要使用经典的后台系统的布局，左边是一个功能侧边栏，右侧是具体的业务组件。

#### 角色控制

这个工单系统需要有完备的 RBAC 角色控制。

我们需要有一个业务系统，这个业务系统大概的分级是 Tenant => Product，然后每个 Product 可以分配给一个或多个不同的 Team，Tenant下属可以有多个Team，Product和Team是多对多的绑定关系，一个 Team 可以处理多个 Product，多个 Product 可以归属于一个 Team，我们可以细化到具体 Product 的某个问题类目来进行客服坐席的分配。

超级管理员可以编辑和控制所有的人员增删和 Tenant、Product 的增删，Tenant 管理员只能控制自己 Tenant 下的团队增删和团队下人员的管理，以及 Tenant 下的产品管理。以此类推，团队管理员只能管理自己的团队。

需要注意的是，各级管理员本身也可以成为客服坐席，是否可以回复工单与RBAC权限无关，客服必然是客服坐席，管理员不一定是客服坐席。每个客服坐席的Profile和其系统账号本身的Profile是分开的，默认二者的名称、邮箱都是同步的，Team及更高级别的管理员可以对客服坐席的Profile进行修改，工单将会分配给客服坐席。

在系统中，“客服”指代最低权限用户，“客服坐席”指代工单的分配对象。

#### 工单处理

我们需要给工单定义常见的一些状态，包括 New、Processing、Replied、Closed。

工单的流转路径是：用户针对某个产品的某一个类目提交工单，我们会根据产品、类目找到对应处理该问题的客服团队（如果没有，则应该是这个产品所属 Tenant 下的默认团队），并从中选择一个处理人进行分配，我们需要保证工单的分配对各个坐席是尽可能均匀的。

工单处理界面主要包括工单的内容（包括基本信息、历史回复等等），还有回复工单的富文本编辑器，和含有各种操作按钮的工具栏。工具栏中包括对工单状态的扭转按钮，例如接单、关单、重新分配、升级等。

Team及以上级别的管理员可以控制客服坐席是否支持重新分配工单，如果支持重新分配，用户可以在当前工单所适应的Team范围内任意寻找其他人进行重新分配。

Product及以上级别的管理员可以配置工单的时效，超出时效的工单在系统内会有告警提示，我们会记录每个客服坐席有多少工单超出了时效，时效包括两部分，一个是多久没有接受处理工单视为超时（重分配人员、升级都会重置这个时间），以及接受处理后多久没有回复会视为超时。对于不同优先级的工单，会有不同的时效。

工单的优先级只有高、中、低三个档位，高优先级的、待处理的工单会出现在工单列表的前部。所有的客服坐席都可以对工单的优先级进行重新分配，修改优先级需要填写具体理由用于记录。

对于已经回复的工单，超过一定时间用户没有继续回复，则会视为关闭。在问题确认办结之后，客服可以在回复后主动关闭工单，已关闭的工单不再支持用户的继续回复。

我们需要保留对工单所有操作的完整操作历史，并且最好在工单详情页面中有地方可以查看该历史。

#### 坐席等级

我们需要引入坐席等级的概念来管理工单，低级别客服坐席在遇到无法处理的工单时可以对工单进行上升。只有 Team 管理员及以上级别的管理员可以分配客服坐席的等级，客服坐席等级的定义需要由 Product 及以上级别的管理员才可以配置。

工单升级的操作类似于重分配，但是它不是指定分配，使用系统在当前Team更高一级坐席的人的氛围中，根据各个客服坐席积压的工单载荷进行自动分配，升级的工单会有单独的升级状态。

#### Dashboard

Dashboard 各级管理员看到的不一样，例如超级管理员看到的是全局分各个Tenant和Product的工单积压情况、总体工单数量、解决数量以及各个工单分类的详细数量数据等，Tenant管理员看到的是Tenant全局和业务下属各个Product的，而Product管理员看到的是Product全局的情况，Team管理员主要看到的是分配给当前Team的工单中各个工单的处理情况，其中Team管理员、Product管理员可以在Dashboard看到当前待处理工单的列表，Tenant管理员和超级管理员只关注统计数据，如果统计数据不足以铺满Dashboard的一屏，我们再考虑加入待解决的工单列表。

对于客服身份的用户，它们的Dashboard主要是当前待处理的工单列表，以及其对应客服坐席的工单积压的情况。

除了Dashboard以外，我们只需要提供一个总体的列表页面来列出所有工单（提供各类筛选项，包括类别、处理状态、时效状态；同时提供各种排序，包括按优先级排序，按过期排序等等）。

#### 工单提交

对应的产品在通过 API 提交工单的时候需要携带必要的用户信息，例如用户的 Email、用户的业务方ID、用户的等级标识（数字）以及可扩展的用户元数据。

#### 客户系统

我们支持在同一个 Tenant 下基于 Email 来构建一个用户系统，同时业务方可以通过提供用户等级来作为一个是否需要提升工单优先级的维度，我们支持针对某个 Product，对高优用户的问题进行优先处理，默认自然的排列维度应该是工单优先级 + 客户优先级。

我们的客户系统支持在同一个 Tenant 下根据用户维度筛查用户在不同产品中遇到的问题，查询客户的必要联系方式等。Tenant 管理员可以看到 Tenant 下属的所有用户，Product 管理员可以看到 Product 关联的所有用户。

#### 工单模版

我们需要支持定义各种工单模版，工单模版主要是给 Product 的工单预设多级的分类，并且可以通过添加一个 Form Schema 来引导用户进行一些填写，这些字段都会以可扩展的 JSON 跟随用户的工单提交输入进工单数据，我们在工单详情的页面需要支持对这些数据的解析和展示。

### API Keys

对于每个Product，Product及以上级别的管理员可以在ToB系统中为Product生成 API Key，单一产品可以有多个 API Key。API Key主要用于SDK和ToC服务的交互。我们所有的SDK方法都应该接驳到 ToC 服务，我们要严格区分服务的 For Public 和 For Private 的界限。

我们需要

### ToC

#### SDK

我们需要提供一个包含查询用户工单、拉取工单模版、提交工单的功能。SDK 本身是对我们针对 Product 提供的 API 进行的封装，这个 SDK 主要是提供给 Product 的后台进行使用，需要是一个完全支持 TypeScript、类型安全的 SDK。

如果 Product 并不会提供工单相关的前端页面，我们可以在 SDK 提供对应的方法生成前端页面的跳转地址，我们需要让 Product 方提供足够的用户信息，由 OnFire 来生成临时的 JWT 供用户进入 OnFire 的 ToC 页面，使用户到达后可以带有具体的产品和用户身份。

#### 前端页面

我们需要提供两个自身无状态的前端页面给用户，一个是工单提交页面，另一个是工单的列表、详情页面，用户可以在该页面查看过往的工单，并对工单的回复进行查看，也可以继续回复工单（如果工单尚未 closed）。

这两个页面只通过 JWT 向后台换取具体的 Product 和用户身份信息，并且基于换取到的信息初始化工单模版、工单列表等，页面本身是无状态的。

前端的工单提交和工单回复都需要接入验证码，我们目前接入 Turnstile。

#### 服务

ToC 的服务端需要提供多个公开的、对外的接口，鉴权模式是 API Key（针对内置前端页面的接口鉴权模式通过 JWT 来验证用户身份）。

需要支持以下一些接口：

1、提交工单（SDK API + 前端页面API）
2、查询用户提交的工单列表
3、查询用户提交的某一工单的详情（需要做好鉴权避免越权问题）
4、查询产品对应的工单模版

## 技术要求

1、所有服务所需的凭据都通过环境变量来设置，包括邮件发送的 SMTP 等，不要定义在代码文件中。
2、你需要保证项目是安全的，不能出现任何的接口越权等情况。
3、你需要保证项目的UI设计是现代化的设计，并且保持UI的紧凑和高效，并且保证所有的UI都是美观的。
4、所有的UI组件都需要优先考虑使用shadcn/ui的组件，并且一律使用zinc主题。
5、我们需要把服务端和前端打包在一起作为一个 Server 上传到 Cloudflare Worker，ElysiaJS。
6、请你良好设计目录结构，我们需要通过多级子目录来妥善管理我们的代码。