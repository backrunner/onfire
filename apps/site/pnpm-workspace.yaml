packages:
  - .
allowBuilds:
  esbuild: true
  sharp: true
  workerd: true

# Remove when upstream minimum versions include these security fixes.
overrides:
  'svedocs>sharp': 0.35.4
  '@sveltejs/kit>cookie': 0.7.2
